# Training

Training pipelines for the CLIP classification head, frequency-artifact classifier, fusion strategy, and calibration parameters.

Training outputs should be versioned as model artefacts through the M2 model registry rather than copied into application source code.