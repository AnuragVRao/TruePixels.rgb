# Infrastructure

Development and deployment support for PostgreSQL, the backend, the frontend, and shared persistent storage.

## Folders

- `docker/`: local container definitions and compose support.
- `postgres/`: database initialization and local configuration.
- `scripts/`: repeatable setup, migration, and operational scripts.