# Backend Tests

Tests local to the FastAPI backend, including module unit tests, route tests, validation tests, and persistence tests.

Cross-module behavior belongs in the root `tests/` folder so the integration contract remains visible to all module owners.