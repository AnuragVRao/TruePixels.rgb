# M2: Image Analysis and Prediction

Implements the CLIP semantic branch, frequency-artifact branch, score fusion, prediction persistence, and model registry.

M2 owns D3 Models and D4 Predictions. It consumes Contract C1 and produces Contract C2. Model registry writes remain inside M2 even when its controls are rendered by M3.