# Shared Contracts

Typed representations of Contracts C1 through C5 from `PRD4_Shared_Interface_Integration.md`.

Expected contracts include `PreprocessedImage`, `InferenceOutput`, `SessionContext`, model registry request/response shapes, and the logging interface.